<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pillskeeper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.friend.EditFriendActivity"></activity>
        <activity android:name=".activity.friend.NewFriendActivity" />
        <activity android:name=".activity.FriendDialog" />
        <activity android:name=".activity.friend.FriendListActivity" />
        <activity
            android:name=".activity.FirstLoginActivity"
            android:label="@string/title_activity_first_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.login.FirstLoginActivity"
            android:label="@string/title_activity_first_login" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.FirstLoginActivity"
            android:label="@string/title_activity_first_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.MainActivity"></activity>
    </application>

</manifest>