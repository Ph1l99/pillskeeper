package com.pillskeeper.enums

/**
 * Enumerative which represents the type of user that can be added to PillsKeeper
 */
enum class RelationEnum(var relation: String) {
    Doctor      ("Doctor"),
    Friend      ("Friend")
}