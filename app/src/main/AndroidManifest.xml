<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pillskeeper">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.appointment.AppointmentActivity" />
        <activity android:name=".activity.pills.MedicinesListActivity" />
        <activity android:name=".activity.pills.reminder.ReminderActivity" />
        <activity android:name=".activity.pills.reminder.AdvancedInfoReminderActivity" />
        <activity android:name=".activity.pills.TextReaderActivity" />
        <activity android:name=".activity.pills.PillsFormActivity" />
        <activity
            android:name=".activity.pills.PillsListActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.SignUp" />
        <activity
            android:name=".activity.friend.FriendListActivity"
            android:label="Friend List" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>