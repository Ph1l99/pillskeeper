<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pillskeeper">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.pills.TextReaderActivity" />
        <activity android:name=".activity.pills.PillsFormActivity" />
        <activity android:name=".activity.pills.PillsListActivity" />
        <activity android:name=".activity.friend.NewFriendActivity" />
        <activity android:name=".activity.SignUp" />
        <activity android:name=".activity.friend.FriendListActivity" />
        <activity
            android:name=".activity.FirstLoginActivity"
            android:label="@string/title_activity_first_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".notifier.ServiceNotifier"
            android:permission="android.permission.BIND_JOB_SERVICE"/>
    </application>

</manifest>